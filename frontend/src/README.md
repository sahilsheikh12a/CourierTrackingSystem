# Frontend Source

Top-level frontend source layout:

- `app/` app shell, routing, and page entry points
- `components/` reusable UI elements
- `features/` feature-specific UI and state
- `services/` API client and data access logic
- `styles/` global styling tokens and base styles
- `utils/` shared frontend helpers
