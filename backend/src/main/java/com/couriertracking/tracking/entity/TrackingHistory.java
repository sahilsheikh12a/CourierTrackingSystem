// JPA entity representing a single tracking event in the shipment history.
// Fields: id, shipmentId, status, location, remarks, updatedBy, updatedAt.
// Records are immutable — never updated after creation.
