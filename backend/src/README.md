# Backend Source

Top-level backend source layout:

- `modules/` for domain modules
- `config/` for environment and app configuration
- `db/` for database access and persistence setup
- `middleware/` for request pipeline logic
- `utils/` for shared helpers
