# Backend Modules

Feature-oriented modules live here.

Suggested ownership:

- `auth/` authentication and authorization
- `booking/` parcel booking workflow
- `tracking/` shipment tracking queries
- `status/` status transition and history updates
- `reporting/` dashboard and metrics logic
- `shared/` cross-module business primitives
